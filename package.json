{
  "name": "jrdelrio-profile",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync": "browser-sync start --proxy 'localhost:4321' --files 'src/**/*.*' --no-ui --no-notify --port 3000"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.11",
    "aos": "^2.3.4",
    "astro": "^5.11.0",
    "tailwindcss": "^4.1.11"
  },
  "devDependencies": {
    "sass": "^1.89.2"
  }
}
